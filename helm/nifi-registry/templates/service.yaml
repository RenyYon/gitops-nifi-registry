apiVersion: v1
kind: Service
metadata:
  name: nifi-registry
spec:
  type: ClusterIP
  selector:
    app: nifi-registry
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 18080
      protocol: TCP
