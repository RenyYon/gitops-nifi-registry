apiVersion: v1
kind: Service
metadata:
  name: nifi-registry
  labels:
    app: nifi-registry
spec:
  type: {{ .Values.service.type }}
  selector:
    app: nifi-registry
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 18080
      protocol: TCP
